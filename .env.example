# Local development only
DATABASE_URL=postgresql://apple@localhost/movies_db

# Claude API (Anthropic) — required for AI scene preview
ANTHROPIC_API_KEY=

# Railway (same project):  DATABASE_URL=${{Postgres.DATABASE_URL}}
# Railway (two projects):   DATABASE_PUBLIC_URL=<paste Public URL from Postgres → Connect>
